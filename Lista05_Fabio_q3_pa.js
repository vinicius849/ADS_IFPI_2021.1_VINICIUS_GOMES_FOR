const prompt = require('prompt-sync')()

function main(){
    const a1 = Number(prompt('Digite aqui o valor do primeiro termo de uma PA: '))
    const N = Number(prompt('Digite aqui sua razão: '))

    for(var a1; i; a1 <= N; i <= N; a1 = a1 + r; i++){
        console.log("a" + i + "=" + a1);

        // Somatório de termos de uma P.A
        sum += a1;
    }

    console.log(`A soma de ${N} termos em sum(somatório) será: `)