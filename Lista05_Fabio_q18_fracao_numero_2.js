const input = require('prompt-sync')()

// Uma questão de P.G em que se trata de uma leitura de números inteiros de 1 a N com razão 1/2 e o denominador (N-1) não se altera,
// porém o numerador muda de uma sucessão 1 a 1, isto é, i++

function main(){
    const N = Number(input('Escreva um número qualquer: '));
    console.log('Soma de termos de frações da questão.')
}
    var razao = (i + 1 / (N-1))
    var sequencia = razao + sequencia

    for(let i = 0; i < N; i++){
    console.log(sequencia)
    }

main()