const input = require('prompt-sync')()

// Uma questão de frações em que se trata de uma leitura de números inteiros de 1 a N com razão 1/2, o numerador não se altera,
// porém o denominador muda de uma sucessão 1 a 1, isto é, i++

function main(){
    const N = Number(input('Escreva um número qualquer: '));
    console.log('Soma de termos de frações da questão.')
}
    var razao = n * (1/2)
    var sequencia = razao + sequencia

    for(let i = 0; i < N; i++){
    console.log(sequencia)
    }

main()