const input = require('prompt-sync')()

// Uma questão de P.G com P.A em que se trata de uma leitura de números inteiros de 1 a N com razão N/2 e o denominador (N-1) não se altera.

function main(){
    const N = Number(input('Escreva um número qualquer: '));
    console.log('Soma de termos de frações da questão.')
}
    var razao = (N / 2 / (i + 1))
    var sequencia = razao + sequencia

    for(let i = 0; i < N; i++){
    console.log(sequencia)
    }

main()